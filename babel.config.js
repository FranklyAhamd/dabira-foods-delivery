// Babel configuration for React Native (mobile app)
// This file should be in the mobile directory, but placing here for reference

module.exports = function(api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
  };
};














